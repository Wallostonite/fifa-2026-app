// Design mode is a platform-only editor feature — no-op in the standalone app.
// (Previously imported from a repo-root ../../../../shared/ path that doesn't
// exist in the deploy build context.)
export {};
